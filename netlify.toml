[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/.netlify/functions/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true

# si tu as une SPA / fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
